# include<stdio.h>
#include<string.h>
void sort(int *ptr,int size)
{
    // worst sort.
    for(int i=;i<size-1;i++)
    {
        for(int j=1;j<=size-1;j++)
        {
            if(arr[i]>arr[j])
            {
                int k=arr[i];
                arr[i]=arr[j]
                arr[j]=k;

            }
        }
    }
}
int main() {
    
    return 0;
}