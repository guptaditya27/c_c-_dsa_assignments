# include<stdio.h>
#include<string.h>
void find_last(char str[][50],int n,char c)
{
    for(int i=0;i<n;i++){
        int j;
        for(j=0;str[i][j];j++)
        {
            
        }
    }
}
int main() {
    char name[10][30];
    for(int i=0;i<10;i++)
    {
        printf("Input String:- ");
        fgets(s[i],30,stdin);
        s[i][strlen(s[i])-1]='\0';
    }

    return 0;
}